'use client';
export default function CollaborationForm({
  
}: {
  onCreated: (data: any) => void;
}) {
  return (
    <form onSubmit={() => {}} className='space-y-4'>
      COLLAB FORMM
    </form>
  );
}
